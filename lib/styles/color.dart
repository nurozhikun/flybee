import 'package:flutter/material.dart';

class AppColor {
  /// 消息框
  //  成功背景
  static const Color successBack = Color(0xFFe3f2db);
  // 失败背景
  static const Color failBack = Color(0xFFE09F9F);
  // 文本
  static const Color successFonts = Color(0xFF1A703B);
  static const Color failFonts = Color(0xFFE62315);

  /// 文本
  // 主要文本_白色
  static const Color primaryElement = Color.fromARGB(255, 255, 255, 255);
  // 主要文本-黑色
  static const Color primaryBlack = Color.fromARGB(255, 20, 20, 20);
  // 次要文本
  static const Color secondaryElement = Color.fromARGB(150, 246, 246, 246);

  // 四级文本
  static const Color fourElement = Color.fromARGB(245, 245, 245, 245);
  // 三级文本
  static const Color thirdElement = Color.fromARGB(255, 120, 120, 120);

  /// 水波纹
  // card点击
  static const Color cardSplash = Color.fromARGB(255, 220, 220, 220);

  /// 分割条
  static const Color dividerContainer = Color.fromARGB(255, 246, 246, 246);

  // checkbox-边框
  static const Color checkboxBorder = Color.fromARGB(255, 100, 100, 100);

  /// ExpandedTile的HeaderSplash
  static const Color expandedTileHeaderSplash =
      Color.fromARGB(220, 220, 220, 220);

  /// axis坐标轴
  static const Color axisText = Color(0xff75729e);

  /// 进度条
  static const Color progress = Color(0xff00bcd4);
  // 任务节点状态
  static const Color nodeState = Color.fromARGB(100, 200, 200, 200);

  /// 边框border
  static const Color border = Color.fromARGB(150, 160, 160, 160);

  /// tabBar进度
  static const Color tabBarPro = Color.fromARGB(250, 255, 127, 0);
  static const Color tabBarDark = Color.fromARGB(150, 160, 160, 160);

  /// lifeCycle
  static const Color lifeGray = Color.fromARGB(255, 160, 160, 160);
  static const Color lifeOrange = Color.fromARGB(255, 255, 127, 0);
  static const Color lifeBlue = Color.fromARGB(255, 72, 118, 255);
  static const Color lifeCyan = Color.fromARGB(255, 18, 150, 250);
  static const Color lifeAmber = Color.fromARGB(255, 202, 76, 38);
  static const Color lifeGreen = Color.fromARGB(255, 0, 200, 50);
  static const Color lifeRed = Color.fromARGB(255, 255, 40, 40);
  static const Color lifeRedLight = Color.fromARGB(255, 150, 40, 40);

  /// area
  static const Color area = Color.fromARGB(255, 20, 120, 250);
}
